# Database - matches docker-compose.yml
DATABASE_URL=postgresql://theepak:password@localhost:5432/asset_manager

# App
PORT=5000
NODE_ENV=production

# JWT - set a strong secret in production
JWT_SECRET=your-secure-jwt-secret-change-in-production

# Public URL for emails/links (use https://your-domain.com when behind nginx)
APP_URL=https://localhost
